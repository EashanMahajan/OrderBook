ANTHROPIC_API_KEY=

# Optional Redis connection (falls back to in-memory if unset)
# REDIS_URL=redis://localhost:6379

# Auto-start the market simulation when the server boots
SIM_AUTO_START=false
SIM_TARGET_PRICE=100.0
